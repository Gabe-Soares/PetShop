- Different user permissions.
- Client user's can addd their pets.
- There must be a page to request/schedule services (like bath, exam, etc).
	* API must return ady schedule.
- Admin user must control the availability of the services, as the list of services that exists.